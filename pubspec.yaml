name: onboarding_view
description: >-
  A modern, highly customizable onboarding for Flutter — glassmorphism, Apple
  liquid glass, minimal & material styles, rich page/indicator animations,
  network/GIF/asset media, and zero-dependency local persistence.
version: 1.0.0
homepage: https://github.com/metekaganmusaak/onboarding_view

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

screenshots:
  - description: "Onboarding View Package Logo"
    path: assets/logo.png
